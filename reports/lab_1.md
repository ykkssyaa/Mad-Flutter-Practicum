# Лабораторная работа №1. Верстка виджетов

В этой работе я сверстал основной экран приложения со списком валют. Сделал карточки валют, строку поиска и общий аккуратный интерфейс в одном стиле. Также разложил элементы по отдельным виджетам, чтобы код было проще поддерживать.

### Что добавить в отчет
- экран списка валют с карточками;
- строка поиска;
- отдельная карточка валюты крупным планом.

